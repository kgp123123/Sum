np.concatenate([a, b], 0):
  concatenate the array, not expand dimension.
np.bincount(x: np.ndarray, weight=None, min_length=0):
  x: a ndarray of position, non-negative int
  return ndarray have x.max() + 1 elements, [pos_0, pos_1, pos_2, ..., pos_max], and each pos is the pos occur time in x
  
  
