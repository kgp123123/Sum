from PIL importImage, ImageFilter
im = Image.open("path")
# im.size is [W, H]


import cv2
im1 = cv2.imread("path")
# im1 is a array, and size is [H, W, C], (RGB)


import imageio
im2 = imageio.imread('path')
# im2 is imageio.core.util.Array: return [H, W, C], (RGBA)
